#!/bin/bash

set -euo pipefail

cname=gui
run="toolbox run --container $cname"

dnf_gkrellm=gkrellm
dnf_sway="wdisplays"

set -x

toolbox create $cname || true
$run sudo dnf install -y $dnf_gkrellm $dnf_sway
